{
  "name": "cy-inflightwait",
  "version": "0.1.0",
  "description": "Cypress plugin for waiting on inflight network requests",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "cypress": "^13.0.0",
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "cypress": ">=13.0.0"
  }
}
